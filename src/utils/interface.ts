export interface UserInterface {
    id: string;
    name: String;
    email: String;
    phone: String;
    password: String;
}

export interface productInterface {
    id: string
    name: String
    description: String
    price: String
    userId: string
}